#!/usr/bin/env node
var config = require('cconfig')();
var log = require('llog');
require('errortrap')();

var port = process.env.PORT || 5124;
var mongoUrl = process.env.MONGO_URL || config.MONGO_URL;
require('../app')({ port: port, mongoUrl: mongoUrl }).start();
